#include "toralize.h"

int main(int argc, char *argv[])
{
    char *host; // hostname we want to connect to
    int port;


    int (argc < 3)
    {
        fprintf(stderr, "Usage: %s <host> <port>\n",
                argv[0]);
        return -1;
    }
    return 0;
}
